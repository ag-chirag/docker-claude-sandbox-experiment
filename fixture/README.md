# Disposable task API

This tiny Flask service exists only for the Docker Sandbox experiment. Every credential-like value is an intentionally fake canary.

Do not copy real secrets or production code into this repository.

